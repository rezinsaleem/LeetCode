            } else {
                start = mid + 1
            if (isBadVersion(mid)) {
                end = mid - 1
            let mid = Math.floor((start + end) / 2)
        while (start <= end) {
        let end = n
        let start = 1;
    return function (n) {
5
