    function inOrder(root) {
        if (root) {
            inOrder(root.left)
        }
            if (n++ < k) res = root.val
            inOrder(root.right)
    }
    let res;
    let n = 0
var kthSmallest = function (root, k) {
 */
    inOrder(root)
[
