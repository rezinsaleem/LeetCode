        if (nums[mid] == target) {
            return mid
    while (left <= right) {
        let mid = Math.floor((left + right) / 2)
    let left = 0;
        }
        if (target > nums[mid]) {
            left = mid + 1
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var searchInsert = function (nums, target) {
    let right = nums.length - 1;
        } else {
[
