/**
 * @param {string} val
 * @return {Object}
 */
var expect = function (val) {
    return {
};
            if (val === val1) return true
        },
            else throw error = 'Not Equal'
        notToBe: (val1) => {
            if (val !== val1) return true
            else throw error = 'Equal'
        }
    }
        toBe: (val1) => {
(
