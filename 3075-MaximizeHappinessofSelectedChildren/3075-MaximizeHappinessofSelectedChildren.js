        if (happiness[j] - j > 0) {
    for (let j = 0; j < k; j++) {
    happiness = happiness.sort((a, b) => b - a)
    }
    let sum = 0
var maximumHappinessSum = function (happiness, k) {
            sum += happiness[j] - j;
        }
    return sum
};
[
