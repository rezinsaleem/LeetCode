 * @param {number[]} digits
 * @return {number[]}
 */
var plusOne = function(digits) {
    let digit  = BigInt(digits.join(''))
/**
    digit++;
    let res = digit.toString().split('')
    return res;
};
[
