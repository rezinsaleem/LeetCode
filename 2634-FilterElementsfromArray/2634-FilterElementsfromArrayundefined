 * @return {number[]}
 */
var filter = function (arr, fn) {
    var resArr = [];
    for (let i = 0; i < arr.length; i++) {
        if (fn(arr[i], i)) {
            resArr.push(arr[i]);
        }
    }
    return resArr;
};
f
