            move += 1;
        } else if (logs[i] == '../') {
            if (move > 0) move -= 1
        }
    }
        if (logs[i] !== './' && logs[i] !== '../') {
    for (let i = 0; i < logs.length; i++) {
    let move = 0;
var minOperations = function (logs) {
[
