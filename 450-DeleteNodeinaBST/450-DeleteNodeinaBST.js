    if (!root) return null;
var deleteNode = function (root, key) {
}
    }
    } else {
        return min(root.left)
        return root.val
    if (!root.left) {
var min = function (root) {
 */
 * @return {TreeNode}
 * @param {number} key
[
