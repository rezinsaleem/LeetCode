            inOrder(root.right)
            sum += root.val
            inOrder(root.left)
        }
            root.val = sum
        if (root) {
    let sum = 0
    const inOrder = ((root) => {
var bstToGst = function (root) {
 */
[
