        if (obj[a] === obj[b]) {
    return nums.sort((a, b) => {
    nums.forEach(countFunc)
    }
        obj[keys] = ++obj[keys] || 1;
    let countFunc = keys => {
            return b - a;
        }
        return obj[a] - obj[b];
[
