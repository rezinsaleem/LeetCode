        }
    })
    inOrder(root1)
    inOrder(root2)
    return arr.sort((a, b) => a - b)
            inOrder(root.left)
            arr.push(root.val)
            inOrder(root.right)
};
        if (root) {
    const inOrder = (root => {
    const arr = []
var getAllElements = function (root1, root2) {
 */
[
